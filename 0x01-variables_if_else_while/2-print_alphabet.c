#include <stdio.h>

/**
 * main - Prints the alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char letters[26] = "abcdefghijklmnopqrstuvwxyz";

	for (int 1 = 0; i < 26; i++)
	{
		putchar(alp[i]);
		putchar("\n");
	}
	return (0);
}
